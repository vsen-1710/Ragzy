import React from 'react';

// This component is no longer needed as we've moved routing to index.js
// Keeping it for compatibility but it's not used in the new router setup
function App() {
  return (
    <div>
      {/* This component is no longer used */}
    </div>
  );
}

export default App;
